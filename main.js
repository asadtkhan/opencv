(function (){
 var names=["jerome", "jolly", "joe", "ashley", "barkha", "catherine"];
 for(var i=0; i<names.length; i++){
 var firstletter=names[i].charAt(0);
 if(names.firstletter==='j'){
     goodbye.speak(names[i])
 }
 else{
    hello.speak(names[i])
 }
}
})();